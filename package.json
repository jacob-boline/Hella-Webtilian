{
  "name": "hella-webtilian",
  "version": "1.0.0",
  "description": "Hella Reptilian band site / Django SPA",
  "private": true,

  "scripts": {
    "django:run": "uv run manage.py runserver 0.0.0.0:8000",
    "django:lr": "uv run manage.py livereload",
    "dev": "concurrently -k -n DJANGO,LR -c green,magenta \"npm:django:run\" \"npm:django:lr\"",
    "lint:css": "stylelint \"**/*.css\"",
    "lint:css:fix": "stylelint \"**/*.css\" --fix"

  },

  "devDependencies": {
    "concurrently": "^9.2.1",

    "stylelint": "^16.26.0",
    "stylelint-order": "^7.0.0",
    "stylelint-use-nesting": "^6.0.0",
    "stylelint-plugin-defensive-css": "^1.0.4",
    "stylelint-config-clean-order": "^8.0.0",
    "stylelint-config-alphabetical-order": "^1.0.0"
  }
}
